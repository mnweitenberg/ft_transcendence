// GAME
export const MIDDLE_LINE_WIDTH = 16;
export const MAX_SCORE = 2;

// ENUMS
export enum ChatState {
	overview,
	personalMessage,
	groupMessage,
}

export enum ModalType {
	userstats,
	alert,
}
